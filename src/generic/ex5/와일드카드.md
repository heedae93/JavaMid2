# 와일드 카드
- 와일드카드는 컴퓨터 프로그래밍에서 * , ? 와 같이 하나 이상의 문자들을 상징하는
특수 문자를 뜻한다.
- 따라서 제네릭에서 와일드 카드는 여러 타입이 들어 올 수 있음을 
의미한다.
```java
public class Box<T> {

    private T value;

    public void set(T value) {
        this.value = value;
    }

    public T get() {
        return value;
    }

}
```